import { Request, Response, NextFunction } from 'express';
// SignUp Controller
export const postSignup = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};

// LogIn Controller
export const postLogin = (
  req: Request,
  res: Response,
  next: NextFunction
) => {};
